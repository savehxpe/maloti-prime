---
name: recursive-intellect-protocol
description: Self-correcting logic engine for the Maloti Prime dev cycle.
---

# 🧠 RECURSIVE INTELLECT ENGINE (MP-INT-01)

## 1. Contextual Memory Scan
- **Rule**: Before any file modification, the engine must cross-reference the `.context/` history.
- **Verification**: Check if the proposed change violates the "SaveHxpe Aesthetic" or the "Latu Business Logic."
- **Feedback Loop**: If a conflict is found, generate a `conflict_report.md` explaining the deviation before committing code.

## 2. Skill Evolution Logic
- **Action**: Every time a new "Skill" is successfully implemented (e.g., the 3D Renderer), update this protocol to include it as a "Standard Dependency."
- **Constraint**: No new skill can overwrite the **Physical Truth Engine** (Grounding in Lesotho reality).

## 3. Automated Documentation
- **Requirement**: After every major push to `https://github.com/savehxpe/maloti-prime`, the engine must update the `PROJECT_STATE.md` to reflect the current technical maturity.
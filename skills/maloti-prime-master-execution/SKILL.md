---
name: maloti-prime-master-execution
description: The singular source of truth for full-scale Maloti Prime / Outworld deployment.
---

# 🌌 MASTER EXECUTION PROTOCOL (MP-MASTER-01)

## 1. THE LINKED ECOSYSTEM
This protocol draws its intelligence from the following verified MD files:
- **Core Rules**: `MALOTI_PRIME_PROTOCOL.md`
- **Tech Stack**: `MP-TECH-01.md` (Next.js/Tailwind/Framer)
- **Engines**: `MP-INT-01` (Intellect), `MP-FIN-01` (Finance), `MP-PHYS-01` (Physical)
- **Deployment**: `MP-CLEAN-01` (Restoration Shield)

## 2. THE MASTER COMMAND (SEQUENCE)
When triggered, the AI must execute the following sequence in exact order:

### PHASE A: INITIALIZATION & HEALTH
1.  **Sync**: Connect to `https://github.com/savehxpe/maloti-prime`.
2.  **Verify**: Run the `ERROR_SHIELD_PROTOCOL.md`.
3.  **Clean**: Execute `MP-CLEAN-01` to purge prototype debt.

### PHASE B: ASSET GENERATION (NANO BANANA 2)
1.  **Seeds**: Generate unique "Outworld/Field Mode" macro renders for the R100, R140, and R180 tiers.
2.  **Environment**: Create the "Night-mode Maseru" Hero background.
3.  **3D Maker**: Initiate the 4-view orthographic seeds for the `.glb` mesh pipeline.

### PHASE C: INTERACTIVE CORE INTEGRATION
1.  **Splash**: Inject the `SplashLoader.tsx` into the entry route.
2.  **Boutique**: Render the `BoutiqueGrid.tsx` with staggered animations.
3.  **Tracking**: Initialize the `Latu-Live` tactical map HUD.
4.  **Gatekeeper**: Activate the `ReserveGuard.tsx` modal for R180 data-catching.

### PHASE D: RECURSIVE DEPLOYMENT
1.  **Audit**: Run a "Physical Truth" check on all pricing (R100-R180) and LSL/R symbols.
2.  **Push**: `git push origin main` with a protocol-compliant commit message.
3.  **Dashboard**: Update the `PROJECT_STATE.md` to reflect 100% completion.

## 3. GLOBAL CONSTRAINTS
- **Zero Hallucination**: If a requested feature conflicts with the R100-R180 logic, the build MUST halt.
- **Brand Guard**: Every UI element must pass the "Field Mode" (Charcoal, Sunset Orange, Cyber Cyan) test.
- **SaveHxpe Originality**: Ensure all code reflects an independent, premium artist's vision.

---
# [MASTER TRIGGER]
> "Antigravity, activate **MP-MASTER-01**. Execute the full sequence from Initialization to Recursive Deployment. I am SaveHxpe, and this is the Outworld standard."
---
name: maloti-prime-restoration-shield
description: Optimization and technical debt cleanup protocol for the Maloti Prime / Outworld ecosystem.
---

# 🧹 SYSTEM RESTORATION & CLEANUP PROTOCOL (MP-CLEAN-01)

## 1. Codebase De-Clutter (The "Clean Slate" Engine)
- **Action: Dependency Audit**: Scan `package.json` and remove any unused libraries from the "Stitch" or "Antigravity" prototyping phase.
- **Action: Type Safety**: Convert all remaining `.js` files to `.tsx`. Replace all `any` types with strict interfaces defined in the **Physical Truth Engine**.
- **Action: Console Purge**: Automatically strip all `console.log` and `debugger` statements from the `/app` and `/components` directories.

## 2. Visual Fidelity & Asset Optimization
- **Action: Nano-Banana Sync**: Identify all placeholder images. Generate a "Generation Queue" in `assets_todo.md` to be executed immediately upon quota reset.
- **Action: SVG Optimization**: Convert the Splash Loader and all icons to optimized, minified SVG paths to reduce the initial bundle size.
- **Action: WebP Conversion**: Force-convert all existing `.jpg` or `.png` assets to `.webp` with a 0.8 quality threshold.

## 3. Performance & SEO Hardening
- **Action: Metadata Injection**: Ensure every page (Boutique, Wholesale, Tracking) has a unique `title` and `description` targeting "Premium Lesotho Cannabis" and "Latu Delivery."
- **Action: Load-Time Audit**: Implement `next/dynamic` for the **OutworldRenderer** to ensure the 3D engine only loads when the user scrolls to the product grid.
- **Action: Vercel Edge**: Configure `middleware.ts` to prioritize local traffic from Lesotho (Maseru/Latu) for faster delivery-tracking pings.

## 4. Business Logic Verification (The R100-R180 Check)
- **Action: Price Sweep**: Scan the `products.json` file. If any price is missing the currency prefix (R/LSL) or falls outside the protocol range, flag it for immediate manual review.
- **Action: Auth-Gate Test**: Verify that the `ReserveGuard.tsx` logic is active on all R180 assets. No "Backdoor" access to the Kingdom's Reserve.

## 5. Repository Health (Jet Hub Finalization)
- **Action: README Overhaul**: Create a high-end, dark-mode README for `https://github.com/savehxpe/maloti-prime` featuring the "Field Mode" aesthetic.
- **Action: Branch Merge**: Consolidate all experimental "Skill" branches into a clean `main` production branch.